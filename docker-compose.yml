services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./api:/app/api
      - ./agent:/app/agent
      - ./pipelines:/app/pipelines
      - ./tools:/app/tools